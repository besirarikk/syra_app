name: syra
description: SYRA - İlişki Danışmanın
publish_to: 'none'

version: 1.0.1+27
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # 🌐 HTTP requests
  http: ^1.2.0

  # 💾 Local storage (CRASH FIX: upgraded to iOS-stable version)
  shared_preferences: ^2.3.2

  # 🔥 Firebase core + Auth + Firestore (stable versions)
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0

  # 🛒 RevenueCat for subscriptions
  purchases_flutter: ^9.9.9

  # 🔗 URL launcher
  url_launcher: ^6.2.5

  # 📅 Date formatting
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # 🖼️ App icon generator
  flutter_launcher_icons: ^0.14.4

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/syra.png"

flutter:
  uses-material-design: true

  assets:
    - assets/icon/syra.png
    - assets/orb/syra_orb.png
